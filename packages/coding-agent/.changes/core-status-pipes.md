- Refinement records and events now carry a machine-readable `source` ("auto" | "manual" | "agent") alongside the existing free-text `trigger`; autonomous status reports `lastInjection` with the continuation reason ("gate_failed" | "missing_terminal_evidence"); RLM child snapshots include `completedAt` for terminal statuses; connection state exposes the read-only `autoRefine` block (settings plus `lastReviewAt`). All additive; daemon schema revision 27.
