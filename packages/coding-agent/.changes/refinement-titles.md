- The /refine refiner now emits an optional concise `title` (≤ 6 words / ~12 CJK characters, normalized and capped at 40 chars) alongside the summary. It is persisted on the harness-state refinement event and the `RefinementResult`, and surfaces on the `prime-agent.refinement` transcript entry, the refinement-outcome message details, the `refine_complete` extension event, and the ACP refinement info. Additive and optional everywhere; older records simply have no title. No daemon schema revision change.
