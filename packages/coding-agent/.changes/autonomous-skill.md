- Added the bundled `autonomous` skill (root sessions only) so the agent can read and switch autonomous mode via `autonomous.get()`, `autonomous.enable(turns=None, tokens=None, time=None, continuations=None)`, and `autonomous.disable()`; limits validate exactly as `/autonomous on` does, the agent gets one grant of unattended mode per session and can only switch off a mode it armed itself, and the status message is emitted at the turn boundary rather than from inside the kernel call.
